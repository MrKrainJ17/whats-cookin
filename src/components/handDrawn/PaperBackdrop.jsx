// Warm-cream paper-fiber background, fixed full-viewport. Used by every
// screen redesigned in the handcrafted language. Pure CSS — see
// `.paper-bg` in index.css.
export default function PaperBackdrop() {
  return <div className="paper-bg" aria-hidden="true" />;
}
